# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

PUT     /binPacking/start           controllers.BinPackingController.start()
GET     /binPacking/steps           controllers.BinPackingController.getSteps(runId, minStep, maxStep)
GET     /binPacking/rawSteps        controllers.BinPackingController.getRawSteps(runId, minStep, maxStep)

GET     /metrics                    controllers.MetricsController.get()

